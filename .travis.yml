language: python

python:
  - 3.6
  - 3.5
before_install:
  - pip install poetry
install:
  - poetry develop

jobs:
  include:

  - stage: Lint
    script:
    - flake8 .

  - stage: Test
    script:
      - python -m pytest tests
